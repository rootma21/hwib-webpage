---
import type { GetStaticPaths } from 'astro';

import Layout from '~/layouts/PageLayout.astro';
import PostListItem from '~/components/ui/PostListItem.astro';
import Headline from '~/components/ui/Headline.astro';

import { blogListRobots, getStaticPathsBlogListAll } from '~/utils/blog';
import { getPermalink } from '~/utils/permalinks';
import { CONTACT_EMAIL } from '~/config/social';
import type { Post } from '~/types';
import NewBadge from '~/components/ui/NewBadge.astro';

type SeriesCard = {
  slug: string;
  title: string;
  description?: string;
  image?: string;
  imageAlt?: string;
  imageFit?: 'cover' | 'contain';
  latestPostDate: number;
  hasNewPost: boolean;
};

export const prerender = true;

export const getStaticPaths = (async () => {
  return await getStaticPathsBlogListAll();
}) satisfies GetStaticPaths;

const { posts, seriesList = [] } = Astro.props as { posts: Post[]; seriesList: SeriesCard[] };

const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);

const categories = [
  {
    label: 'Quick Takes',
    href: getPermalink('quick-take', 'category'),
    description: 'Short, punchy perspectives on bioinformatics news and ideas.',
    comingSoon: false,
    borderColor: 'border-[rgb(1_97_239)]',
    textColor: 'text-[rgb(1_97_239)]',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 opacity-80">
      <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 13.5l10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75z" />
    </svg>`,
  },
  {
    label: 'Deep Dives',
    href: getPermalink('deep-dive', 'category'),
    description: 'In-depth explorations of complex topics in computational biology.',
    comingSoon: false,
    borderColor: 'border-[rgb(109_40_217)]',
    textColor: 'text-[rgb(109_40_217)]',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 opacity-80">
      <path stroke-linecap="round" stroke-linejoin="round" d="M9.75 3.104v5.714a2.25 2.25 0 01-.659 1.591L5 14.5M9.75 3.104c-.251.023-.501.05-.75.082m.75-.082a24.301 24.301 0 014.5 0m0 0v5.714c0 .597.237 1.17.659 1.591L19.8 15.3M14.25 3.104c.251.023.501.05.75.082M19.8 15.3l-1.57.393A9.065 9.065 0 0112 15a9.065 9.065 0 00-6.23-.693L5 14.5m14.8.8l1.402 1.402c1.232 1.232.65 3.318-1.067 3.611A48.309 48.309 0 0112 21a48.309 48.309 0 01-8.135-.687c-1.718-.293-2.3-2.379-1.067-3.61L5 14.5" />
    </svg>`,
  },
  {
    label: 'Tutorials',
    href: getPermalink('tutorial', 'category'),
    description: 'Step-by-step guides to build your bioinformatics skills.',
    comingSoon: false,
    borderColor: 'border-[#e36d1d]',
    textColor: 'text-[#e36d1d]',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 opacity-80">
      <path stroke-linecap="round" stroke-linejoin="round" d="M17.25 6.75L22.5 12l-5.25 5.25m-10.5 0L1.5 12l5.25-5.25m7.5-3l-4.5 16.5" />
    </svg>`,
  },
  {
    label: 'Interviews',
    href: getPermalink('interview', 'category'),
    description: 'Conversations with leaders and innovators in the field.',
    comingSoon: false,
    borderColor: 'border-[#0077b5]',
    textColor: 'text-[#0077b5]',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 opacity-80">
      <path stroke-linecap="round" stroke-linejoin="round" d="M18 18.72a9.094 9.094 0 003.741-.479 3 3 0 00-4.682-2.72m.94 3.198l.001.031c0 .225-.012.447-.037.666A11.944 11.944 0 0112 21c-2.17 0-4.207-.576-5.963-1.584A6.062 6.062 0 016 18.719m12 0a5.971 5.971 0 00-.941-3.197m0 0A5.995 5.995 0 0012 12.75a5.995 5.995 0 00-5.058 2.772m0 0a3 3 0 00-4.681 2.72 8.986 8.986 0 003.74.477m.94-3.197a5.971 5.971 0 00-.94 3.197M15 6.75a3 3 0 11-6 0 3 3 0 016 0zm6 3a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0zm-13.5 0a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0z" />
    </svg>`,
  },
];

const metadata = {
  title: 'Blog',
  robots: {
    index: blogListRobots?.index,
    follow: blogListRobots?.follow,
  },
  openGraph: {
    type: 'blog',
  },
};
---

<Layout metadata={metadata}>
  <section class="px-4 sm:px-6 lg:px-8 pt-3 pb-12 mx-auto max-w-6xl">
    <Headline
      title="Blogs"
      subtitle="Enjoy news, tutorials, resources and other interesting content related to bioinformatics"
      classes={{ container: 'my-2 md:my-3 max-w-4xl' }}
    />
    <!-- Search + Filter controls -->
    <div class="flex flex-col sm:flex-row gap-3 mb-8">
      <!-- Search Bar -->
      <div class="relative flex-1">
        <svg
          class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400 dark:text-gray-500 pointer-events-none"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z"></path>
        </svg>
        <input
          id="search-input"
          type="search"
          placeholder="Search by title, author, tag, or series…"
          class="w-full pl-12 pr-4 py-4 text-base rounded-xl border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-slate-900 text-gray-900 dark:text-slate-100 placeholder-gray-400 dark:placeholder-gray-500 focus:ring-2 focus:ring-primary focus:border-primary transition-colors duration-200 shadow-sm"
          autocomplete="off"
        />
      </div>

      <!-- Series filter checkbox -->
      <label
        class="inline-flex items-center gap-2 px-6 py-4 text-gray-700 dark:text-slate-300 font-medium text-base cursor-pointer transition-colors duration-200 whitespace-nowrap select-none"
      >
        <input id="series-filter-btn" type="checkbox" class="sr-only" />
        <span
          id="series-checkbox-box"
          class="w-5 h-5 rounded border-2 border-gray-300 dark:border-gray-600 flex items-center justify-center transition-colors duration-200 flex-shrink-0"
        >
          <svg
            id="series-checkbox-check"
            class="w-3 h-3 hidden text-white"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            stroke-width="3"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"></path>
          </svg>
        </span>
        Series only
      </label>
    </div>

    <!-- Category Cards (shown when not searching) -->
    <div id="category-cards" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      {
        categories.map((cat) =>
          cat.comingSoon ? (
            <div
              class={`relative flex flex-col items-center text-center p-8 rounded-2xl border-4 ${cat.borderColor} bg-white dark:bg-slate-900 cursor-not-allowed opacity-60`}
            >
              <div class={`${cat.textColor} mb-4`} set:html={cat.icon} />
              <h3 class={`text-xl font-bold mb-2 ${cat.textColor}`}>{cat.label}</h3>
              <p class="text-sm text-gray-500 dark:text-gray-400 mb-4">{cat.description}</p>
              <span class="text-xs font-semibold uppercase tracking-wide bg-gray-200 dark:bg-gray-700 text-gray-500 dark:text-gray-400 px-3 py-1 rounded-full">
                Coming Soon
              </span>
            </div>
          ) : (
            <a
              href={cat.href}
              class={`flex flex-col items-center text-center p-8 rounded-2xl border-4 ${cat.borderColor} bg-white dark:bg-slate-900 hover:shadow-lg transition-shadow duration-200 group`}
            >
              <div
                class={`${cat.textColor} mb-4 group-hover:scale-110 transition-transform duration-200`}
                set:html={cat.icon}
              />
              <h3 class={`text-xl font-bold mb-2 ${cat.textColor}`}>{cat.label}</h3>
              <p class="text-sm text-gray-500 dark:text-gray-400">{cat.description}</p>
            </a>
          )
        )
      }
    </div>

    <!-- Search Results (hidden until searching or series filter active) -->
    <div id="search-results-section" class="hidden">
      <!-- Sort button -->
      <div class="flex justify-end mb-4">
        <button
          id="sort-btn"
          data-order="desc"
          class="inline-flex items-center gap-2 px-6 py-4 rounded-xl border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-slate-900 text-gray-700 dark:text-slate-300 font-medium text-base hover:border-primary hover:text-primary dark:hover:border-primary dark:hover:text-primary transition-colors duration-200 shadow-sm whitespace-nowrap"
        >
          <svg
            id="sort-icon"
            xmlns="http://www.w3.org/2000/svg"
            class="w-5 h-5"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            stroke-width="2"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 4h13M3 8h9M3 12h5m10 0l-4 4m4-4l-4-4"></path>
          </svg>
          <span id="sort-label">Newest first</span>
        </button>
      </div>

      <p id="results-count" aria-live="polite" class="text-sm text-muted dark:text-slate-400 mb-6">
        Showing <span id="results-visible"></span> of {posts.length} posts
      </p>

      <ul id="posts-list">
        {
          /* Series cards — hidden by default, shown only in "series only" mode */
          seriesList.map((s) => {
            const seriesLink = getPermalink(s.slug, 'series');
            return (
              <li class="mb-8 md:mb-12" data-series-card data-date={s.latestPostDate} style="display:none">
                <div class="relative max-w-md mx-auto md:max-w-none">
                  <div class="absolute inset-0 translate-x-2 translate-y-2 rounded bg-gray-100 dark:bg-slate-700/60" />
                  <div class="absolute inset-0 translate-x-1 translate-y-1 rounded bg-gray-200/80 dark:bg-slate-700/80" />
                  {s.hasNewPost && <NewBadge />}
                  <a
                    href={seriesLink}
                    class={`group relative grid gap-4 md:gap-6 ${s.image ? 'md:grid-cols-2' : ''} bg-white dark:bg-slate-800 rounded shadow-lg hover:shadow-xl transition-shadow duration-200`}
                  >
                    {s.image && (
                      <div class="relative h-0 pb-[56.25%] md:pb-0 md:h-56 lg:h-64 overflow-hidden bg-white rounded-tl rounded-bl">
                        <img
                          src={s.image}
                          alt={s.imageAlt ?? s.title}
                          class={`absolute inset-0 w-full h-full rounded-tl rounded-bl transition-transform duration-300 ${s.imageFit === 'contain' ? 'object-contain p-3' : 'object-cover group-hover:scale-105'}`}
                          loading="lazy"
                        />
                      </div>
                    )}
                    <div class={`mt-2 ${s.image ? 'pr-4 pb-4' : 'p-4'}`}>
                      <div class="mb-1.5 flex items-center gap-2">
                        <span class="inline-flex items-center gap-1 text-xs font-semibold px-2.5 py-0.5 rounded-full bg-purple-100 dark:bg-purple-900/40 text-purple-700 dark:text-purple-300">
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="w-3 h-3"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                            stroke-width="2"
                          >
                            <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0l-4-4m4 4l-4 4" />
                          </svg>
                          Series
                        </span>
                      </div>
                      <h2 class="text-xl sm:text-2xl font-bold leading-tight mb-2 font-heading text-gray-900 dark:text-slate-100 group-hover:text-primary dark:group-hover:text-blue-700 transition ease-in duration-200">
                        {s.title}
                      </h2>
                      {s.description && <p class="text-muted dark:text-slate-300 text-lg">{s.description}</p>}
                      <p class="text-sm text-purple-600 dark:text-purple-400 font-medium mt-3">View all posts →</p>
                    </div>
                  </a>
                </div>
              </li>
            );
          })
        }
        {
          posts.map((post) => {
            const authorNames = post.authors ? post.authors.map((a) => a.name).join(' ') : '';
            const seriesTitle = post.series?.title || '';
            const tagTitles = post.tags
              ? post.tags.map((t) => t.title + ' ' + t.slug.replace(/-/g, ' ')).join(' ')
              : '';
            const searchData = [post.title, authorNames, seriesTitle, tagTitles].join(' ').toLowerCase();
            const isNew = new Date(post.publishDate) >= thirtyDaysAgo;
            const dateMs = new Date(post.publishDate).getTime();
            const inSeries = post.series ? 'true' : 'false';

            return (
              <li class="mb-4 md:mb-6" data-search={searchData} data-date={dateMs} data-in-series={inSeries}>
                <PostListItem post={post} isNew={isNew} />
              </li>
            );
          })
        }
      </ul>

      <p id="no-results" class="hidden text-center text-gray-500 dark:text-gray-400 py-12 text-lg">
        No posts match your search.
      </p>
    </div>

    <p class="text-sm text-muted dark:text-slate-400 text-center max-w-4xl mx-auto mt-12 mb-0">
      Have content you want to submit? Contact <a href={`mailto:${CONTACT_EMAIL}`} class="text-primary hover:underline"
        >{CONTACT_EMAIL}</a
      > and fill out our <a
        href="https://docs.google.com/forms/d/e/1FAIpQLSdmsqWJ7oHRC8kK7NIMgyGuGegNsIY1yVQpMeV8VTHXvsPstA/viewform?usp=header"
        target="_blank"
        rel="noopener noreferrer"
        class="text-primary hover:underline">Google Form</a
      >.
    </p>
  </section>
</Layout>

<script>
  const searchInput = document.getElementById('search-input') as HTMLInputElement;
  const sortBtn = document.getElementById('sort-btn') as HTMLButtonElement;
  const sortLabel = document.getElementById('sort-label') as HTMLSpanElement;
  const sortIcon = document.getElementById('sort-icon') as unknown as SVGElement;
  const seriesFilterBtn = document.getElementById('series-filter-btn') as HTMLInputElement;
  const seriesCheckboxBox = document.getElementById('series-checkbox-box') as HTMLSpanElement;
  const seriesCheckboxCheck = document.getElementById('series-checkbox-check') as unknown as SVGElement;
  const postsList = document.getElementById('posts-list') as HTMLUListElement;
  const resultsVisible = document.getElementById('results-visible');
  const noResults = document.getElementById('no-results');
  const searchResultsSection = document.getElementById('search-results-section');
  const categoryCards = document.getElementById('category-cards');

  const iconDesc = `<path stroke-linecap="round" stroke-linejoin="round" d="M3 4h13M3 8h9M3 12h5m10 0l-4 4m4-4l-4-4" />`;
  const iconAsc = `<path stroke-linecap="round" stroke-linejoin="round" d="M3 4h13M3 8h9M3 12h5m10 8l-4-4m4 4l-4-4" />`;

  function isSeriesOnly() {
    return seriesFilterBtn.checked;
  }

  function updateCheckboxVisual() {
    if (seriesFilterBtn.checked) {
      seriesCheckboxBox.classList.remove('border-gray-300', 'dark:border-gray-600');
      seriesCheckboxBox.classList.add('bg-purple-600', 'border-purple-600');
      seriesCheckboxCheck.classList.remove('hidden');
    } else {
      seriesCheckboxBox.classList.add('border-gray-300', 'dark:border-gray-600');
      seriesCheckboxBox.classList.remove('bg-purple-600', 'border-purple-600');
      seriesCheckboxCheck.classList.add('hidden');
    }
  }

  function applySort(order: 'asc' | 'desc') {
    const items = Array.from(postsList.querySelectorAll<HTMLLIElement>('li[data-date]'));
    items.sort((a, b) => {
      const da = Number(a.getAttribute('data-date'));
      const db = Number(b.getAttribute('data-date'));
      return order === 'desc' ? db - da : da - db;
    });
    items.forEach((li) => postsList.appendChild(li));
  }

  function getSeriesItems() {
    return Array.from(postsList.querySelectorAll<HTMLLIElement>('li[data-series-card]'));
  }

  function update() {
    const query = searchInput.value.trim().toLowerCase();
    const seriesOnly = isSeriesOnly();
    const items = Array.from(postsList.querySelectorAll<HTMLLIElement>('li[data-search]'));
    let visibleCount = 0;

    const showResults = !!(query || seriesOnly);
    searchResultsSection!.classList.toggle('hidden', !showResults);
    categoryCards!.classList.toggle('hidden', showResults);

    // Post items: hide all when series-only, otherwise filter by search
    items.forEach((li) => {
      if (seriesOnly) {
        li.style.display = 'none';
      } else {
        const matches = !query || (li.getAttribute('data-search') || '').includes(query);
        li.style.display = matches ? '' : 'none';
        if (matches) visibleCount++;
      }
    });

    // Series cards: show only in series-only mode, hide when searching
    getSeriesItems().forEach((li) => {
      li.style.display = seriesOnly ? '' : 'none';
    });

    const totalVisible = seriesOnly ? getSeriesItems().length : visibleCount;
    noResults!.classList.toggle('hidden', totalVisible > 0);
    if (query && !seriesOnly) {
      resultsVisible!.textContent = String(visibleCount);
    }

    const params = new URLSearchParams();
    if (query) params.set('q', query);
    const newUrl = params.toString() ? `${window.location.pathname}?${params.toString()}` : window.location.pathname;
    window.history.replaceState({}, '', newUrl);
  }

  seriesFilterBtn.addEventListener('change', () => {
    updateCheckboxVisual();
    update();
  });

  sortBtn.addEventListener('click', () => {
    const current = sortBtn.getAttribute('data-order') as 'asc' | 'desc';
    const next = current === 'desc' ? 'asc' : 'desc';
    sortBtn.setAttribute('data-order', next);
    sortLabel.textContent = next === 'desc' ? 'Newest first' : 'Oldest first';
    sortIcon.innerHTML = next === 'desc' ? iconDesc : iconAsc;
    applySort(next);
  });

  searchInput.addEventListener('input', update);

  // Restore search query from URL
  const params = new URLSearchParams(window.location.search);
  const q = params.get('q');
  if (q) {
    searchInput.value = q;
    update();
  }
</script>
