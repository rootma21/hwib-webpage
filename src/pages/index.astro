---
import Layout from '~/layouts/PageLayout.astro';
import { SITE } from '~/config/site/config.js';
import { DISCORD_URL, LINKEDIN_URL, INSTAGRAM_URL, LUMA_URL, CONTACT_EMAIL } from '~/config/social';
// import Hero2 from '~/components/widgets/Hero2.astro'; // restore with mentorship section below
import Features2 from '~/components/widgets/Features2.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';
//import eventImg from '~/../public/photos/2025/WIB_10Year_group_photo1.jpg'; // adjust path as needed
//import AddToCalendarButton from '~/components/events/AddToCalendarButton.tsx';
import Button from '~/components/ui/Button.astro';
import Card from '~/components/ui/Card.astro';
import UpcomingEvents from '~/components/widgets/UpcomingEvents.astro';
import { findLatestNewsletters } from '~/utils/newsletter';
import { findLatestPosts } from '~/utils/blog';
import { getCollection } from 'astro:content';
import { APP_NEWSLETTER, APP_BLOG } from 'astrowind:config';
import { getBlogPermalink, getPermalink } from '~/utils/permalinks';
import Image from '~/components/common/Image.astro';
import NewBadge from '~/components/ui/NewBadge.astro';
import NewsletterSignup from '~/components/newsletter/NewsletterSignup.astro';
import { Icon } from 'astro-icon/components';

const latestNewsletter = APP_NEWSLETTER.isEnabled ? (await findLatestNewsletters({ count: 1 }))[0] : null;
const allLatestPosts = APP_BLOG.isEnabled ? (await findLatestPosts({ count: 20 })) || [] : [];
const latestPodcast = allLatestPosts.find((p) => p.category?.slug === 'podcast') || null;
const nonPodcastPosts = allLatestPosts.filter((p) => p.category?.slug !== 'podcast');

// Build a map of series slug → series image/alt from the series collection
const seriesEntries = await getCollection('series');
const seriesImageMap = new Map<string, { image?: string; imageAlt?: string; imageFit?: string }>();
seriesEntries.forEach((entry) => {
  const slug = entry.id.replace(/\.md$/, '');
  seriesImageMap.set(slug, { image: entry.data.image, imageAlt: entry.data.imageAlt, imageFit: entry.data.imageFit });
});

// Build a map of series slug → 2 most recent posts
const seriesPostsMap = new Map<string, typeof nonPodcastPosts>();
nonPodcastPosts.forEach((p) => {
  if (p.series?.slug) {
    const existing = seriesPostsMap.get(p.series.slug) ?? [];
    if (existing.length < 2) seriesPostsMap.set(p.series.slug, [...existing, p]);
  }
});

const seenSeries = new Set<string>();
const latestMediaPosts = nonPodcastPosts
  .filter((p) => {
    if (p.series?.slug) {
      if (seenSeries.has(p.series.slug)) return false;
      seenSeries.add(p.series.slug);
    }
    return true;
  })
  .slice(0, 4);

const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);

const newsletterDate = latestNewsletter?.publishDate ? new Date(latestNewsletter.publishDate) : new Date(0);
const podcastDate = latestPodcast?.publishDate ? new Date(latestPodcast.publishDate) : new Date(0);
const podcastFirst = podcastDate > newsletterDate;

const metadata = {
  title: SITE.title,
  description: SITE.description,
};

// Hidden for now per HWIB request — flip to true to restore either section.
const showCommunitySection = false;
const showCallForSpeakers = false;
---

<Layout metadata={metadata}>
  <!-- Hero Introduction Section -->
  <section class="py-16 md:py-20 bg-white dark:bg-dark">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h1 class="text-4xl sm:text-5xl md:text-6xl dark:text-gray-100 font-bold mb-4 leading-tight">
        Houston Women in <span class="text-accent dark:text-gray-100 highlight">Bioinformatics</span>
      </h1>
      <p class="text-lg sm:text-xl text-gray-700 dark:text-gray-300">
        Advancing women in bioinformatics by creating networks of support, collaboration, and empowerment.
      </p>
      <div class="flex justify-center gap-6 mt-6">
        <a
          href={DISCORD_URL}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Join our Discord community"
          class="text-gray-500 hover:text-primary dark:text-gray-400 dark:hover:text-white transition-colors duration-200"
        >
          <Icon name="tabler:brand-discord" class="w-8 h-8" />
        </a>
        <a
          href={LINKEDIN_URL}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Connect with us on LinkedIn"
          class="text-gray-500 hover:text-primary dark:text-gray-400 dark:hover:text-white transition-colors duration-200"
        >
          <Icon name="tabler:brand-linkedin" class="w-8 h-8" />
        </a>
        <a
          href={INSTAGRAM_URL}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Follow us on Instagram"
          class="text-gray-500 hover:text-primary dark:text-gray-400 dark:hover:text-white transition-colors duration-200"
        >
          <Icon name="tabler:brand-instagram" class="w-8 h-8" />
        </a>
        <a
          href={LUMA_URL}
          target="_blank"
          rel="noopener noreferrer"
          aria-label="View our events on Luma"
          class="text-gray-500 hover:text-primary dark:text-gray-400 dark:hover:text-white transition-colors duration-200"
        >
          <Icon name="tabler:calendar-event" class="w-8 h-8" />
        </a>
      </div>
    </div>
  </section>

  {
    /* Career Mentorship Program Call to Action — uncomment to restore for next cohort announcement
  <section class="py-10 md:py-14 bg-gray-50 dark:bg-slate-800">
    <div class="max-w-7xl mx-auto px-4 md:px-6">
      <Hero2
        title="Career Mentorship Program"
        subtitle="Empowering women in bioinformatics to advance into leadership roles across academia and industry."
        tagline="2025-2026 Cohort Matches Are Out!"
        imageBefore={false}
        imageGap="lg:gap-8"
        padding="py-8 md:py-10 lg:py-0"
        image={{
          src: 'https://images.unsplash.com/photo-1553028826-f4804a6dba3b?q=80&w=870&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
          alt: 'A group of women work together at a wooden table with laptops and papers.',
        }}
        imageCitation='Photo by <a href="https://unsplash.com/@cowomen?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">CoWomen</a> on <a href="https://unsplash.com/photos/a-group-of-people-sitting-around-a-wooden-table-ZKHksse8tUU?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Unsplash</a>'
      >
        <Fragment slot="content">
          <div class="text-center lg:text-left mb-6">
            <p class="text-lg text-gray-700 dark:text-gray-300 mb-4">
              The mentor-mentee matches for our 2025-2026 cohort have been sent out! Check your email for your match
              details and next steps.
            </p>
          </div>
          <div class="mt-6 text-center lg:text-left">
            <a
              href="/resources/career-mentorship"
              class="text-primary dark:text-blue-400 hover:underline text-sm font-medium"
            >
              Learn more about the program →
            </a>
          </div>
        </Fragment>
      </Hero2>
    </div>
  </section>
  */
  }

  <!-- Upcoming Events Section -->
  <section class="py-10 md:py-14 bg-blue-50 dark:bg-blue-950 dark:text-gray-200">
    <div class="max-w-7xl mx-auto px-4 md:px-6">
      <UpcomingEvents />
    </div>
  </section>

  <!-- From Our Committees Section (Newsletter + Podcast) -->
  <section class="py-10 md:py-14 bg-white dark:bg-dark dark:text-gray-200">
    <div class="max-w-7xl mx-auto px-4 md:px-6">
      <h2 class="text-3xl font-bold tracking-tight sm:text-4xl mb-8 text-center">Updates</h2>
      <div class="grid md:grid-cols-2 gap-8 md:items-stretch">
        <!-- Newsletter Column -->
        {
          latestNewsletter &&
            (() => {
              const isRemoteNewsletterImage =
                typeof latestNewsletter.image === 'string' &&
                (latestNewsletter.image.startsWith('http') ||
                  latestNewsletter.image.startsWith('/assets/') ||
                  latestNewsletter.image.startsWith('/blog_images/') ||
                  latestNewsletter.image.startsWith('/photos/'));
              return (
                <article
                  aria-labelledby="latest-newsletter-heading"
                  class={`flex flex-col h-full ${podcastFirst ? 'md:order-last' : ''}`}
                >
                  <h3 id="latest-newsletter-heading" class="text-2xl font-bold mb-4">
                    Communications
                  </h3>
                  <NewsletterSignup />
                  <div class="relative flex flex-col flex-1">
                    {new Date(latestNewsletter.publishDate) >= thirtyDaysAgo && <NewBadge />}
                    <Card variant="blog" padding="p-0" class="flex flex-col flex-1">
                      {latestNewsletter.image && (
                        <a
                          href={latestNewsletter.permalink}
                          aria-label={`Read newsletter: ${latestNewsletter.excerpt?.slice(0, 60) || latestNewsletter.title}`}
                          class="block"
                        >
                          {isRemoteNewsletterImage ? (
                            <img
                              src={latestNewsletter.image}
                              alt={
                                latestNewsletter.imageAlt ||
                                latestNewsletter.excerpt?.slice(0, 100) ||
                                latestNewsletter.title
                              }
                              width="600"
                              height="400"
                              class="w-full h-64 object-cover"
                              loading="lazy"
                            />
                          ) : (
                            <Image
                              src={latestNewsletter.image}
                              alt={
                                latestNewsletter.imageAlt ||
                                (latestNewsletter.excerpt ? latestNewsletter.excerpt.slice(0, 100) : null) ||
                                latestNewsletter.title ||
                                'Newsletter image'
                              }
                              class="w-full h-64 object-cover"
                              widths={[400, 600]}
                              width={600}
                              height={400}
                              sizes="(max-width: 768px) 100vw, 50vw"
                              loading="lazy"
                            />
                          )}
                        </a>
                      )}
                      <div class="p-6">
                        <h4 class="text-xl font-bold mb-3">
                          <a
                            href={latestNewsletter.permalink}
                            class="hover:text-primary dark:hover:text-blue-700 transition ease-in duration-200"
                          >
                            {latestNewsletter.title}
                          </a>
                        </h4>
                        {latestNewsletter.excerpt && (
                          <p class="text-muted dark:text-slate-300 text-base mb-4">{latestNewsletter.excerpt}</p>
                        )}
                        <Button variant="link" href={latestNewsletter.permalink}>
                          Read more »
                        </Button>
                      </div>
                    </Card>
                  </div>
                </article>
              );
            })()
        }

        <!-- Latest Podcast Column -->
        {
          latestPodcast &&
            (() => {
              const isRemotePodcastImage =
                typeof latestPodcast.image === 'string' &&
                (latestPodcast.image.startsWith('http') ||
                  latestPodcast.image.startsWith('/assets/') ||
                  latestPodcast.image.startsWith('/blog_images/') ||
                  latestPodcast.image.startsWith('/photos/'));
              return (
                <article
                  aria-labelledby="latest-podcast-heading"
                  class={`flex flex-col h-full ${podcastFirst ? 'md:order-first' : ''}`}
                >
                  <h3 id="latest-podcast-heading" class="text-2xl font-bold mb-4">
                    Podcast
                  </h3>
                  <div class="relative flex flex-col flex-1">
                    {new Date(latestPodcast.publishDate) >= thirtyDaysAgo && <NewBadge />}
                    <Card variant="blog" padding="p-0" class="flex flex-col flex-1">
                      {latestPodcast.image && (
                        <a href={latestPodcast.permalink} class="block">
                          {isRemotePodcastImage ? (
                            <img
                              src={latestPodcast.image}
                              alt={latestPodcast.imageAlt || latestPodcast.title}
                              class="w-full h-64 object-contain"
                              loading="lazy"
                            />
                          ) : (
                            <Image
                              src={latestPodcast.image}
                              alt={latestPodcast.imageAlt || latestPodcast.title || 'Podcast image'}
                              class="w-full h-64 object-contain"
                              widths={[400, 600]}
                              width={600}
                              height={600}
                              sizes="(max-width: 768px) 100vw, 50vw"
                              loading="lazy"
                            />
                          )}
                        </a>
                      )}
                      <div class="p-6">
                        <p class="text-xs font-semibold uppercase tracking-wide text-muted dark:text-slate-400 mb-2">
                          Latest Episode
                        </p>
                        <h4 class="text-xl font-bold mb-3">
                          <a
                            href={latestPodcast.permalink}
                            class="hover:text-primary dark:hover:text-blue-700 transition ease-in duration-200"
                          >
                            {latestPodcast.title}
                          </a>
                        </h4>
                        {latestPodcast.excerpt && (
                          <p class="text-muted dark:text-slate-300 text-base mb-4">{latestPodcast.excerpt}</p>
                        )}
                        <Button variant="link" href={latestPodcast.permalink}>
                          Listen now »
                        </Button>
                      </div>
                    </Card>
                  </div>
                </article>
              );
            })()
        }
      </div>
    </div>
  </section>

  <!-- Latest from Our Community Section (blogs + videos) — hidden for now, flip showCommunitySection to restore -->
  {
    showCommunitySection && (
      <section class="py-10 md:py-14 bg-gray-50 dark:bg-slate-800 dark:text-gray-200">
        <div class="max-w-7xl mx-auto px-4 md:px-6">
          <h2 class="text-3xl font-bold tracking-tight sm:text-4xl mb-4 text-center">Latest from Our Community</h2>
          <p class="text-center text-gray-500 dark:text-gray-400 mb-8">
            Dive into the latest blogs, videos, and conversations from our members
          </p>
          <div class="grid grid-cols-2 gap-6">
            {latestMediaPosts.map((post) => {
              const seriesMeta = post.series?.slug ? seriesImageMap.get(post.series.slug) : undefined;
              const cardImage = seriesMeta?.image ?? post.image;
              const cardImageAlt = seriesMeta?.imageAlt ?? post.imageAlt;
              const cardImageFit = post.imagePosition === 'contain' ? 'contain' : (seriesMeta?.imageFit ?? 'cover');
              const objectPosition =
                post.imagePosition === 'top' ? 'top' : post.imagePosition === 'bottom' ? 'bottom' : 'center';
              const isRemoteImage =
                typeof cardImage === 'string' &&
                (cardImage.startsWith('http') ||
                  cardImage.startsWith('/assets/') ||
                  cardImage.startsWith('/blog_images/'));
              const isNew = new Date(post.publishDate) >= thirtyDaysAgo;
              return (
                <div class="relative">
                  {isNew && <NewBadge />}
                  <Card variant="blog" padding="p-0">
                    {cardImage && (
                      <a href={post.series ? `/series/${post.series.slug}` : post.permalink} class="block">
                        {isRemoteImage ? (
                          <img
                            src={cardImage}
                            alt={cardImageAlt}
                            class={`w-full h-48 object-${cardImageFit}`}
                            style={`object-position: ${objectPosition};`}
                            loading="lazy"
                          />
                        ) : (
                          <Image
                            src={cardImage}
                            alt={cardImageAlt || post.title || 'Post image'}
                            class={`w-full h-48 object-${cardImageFit}`}
                            style={`object-position: ${objectPosition};`}
                            widths={[400, 600]}
                            width={600}
                            height={400}
                            sizes="(max-width: 768px) 50vw, 50vw"
                            loading="lazy"
                          />
                        )}
                      </a>
                    )}
                    <div class="p-3">
                      {post.series ? (
                        <>
                          <span class="inline-block text-xs font-semibold text-primary dark:text-blue-400 uppercase tracking-wide mb-1">
                            Series: {post.series.title}
                          </span>
                          <ul class="mt-1 space-y-1">
                            {(seriesPostsMap.get(post.series.slug) ?? []).map((sp) => (
                              <li class="text-xs">
                                <a
                                  href={sp.permalink}
                                  class="font-semibold hover:text-primary dark:hover:text-blue-400 transition ease-in duration-200"
                                >
                                  {sp.title}
                                </a>
                              </li>
                            ))}
                          </ul>
                        </>
                      ) : (
                        <>
                          <h4 class="text-sm font-bold leading-tight mb-1">
                            <a
                              href={post.permalink}
                              class="hover:text-primary dark:hover:text-blue-700 transition ease-in duration-200"
                            >
                              {post.title}
                            </a>
                          </h4>
                          {post.excerpt && (
                            <p class="text-muted dark:text-slate-300 text-xs line-clamp-2">{post.excerpt}</p>
                          )}
                        </>
                      )}
                    </div>
                  </Card>
                </div>
              );
            })}
          </div>
          <div class="text-center mt-6">
            <Button variant="link" href={getBlogPermalink()}>
              View all posts »
            </Button>
          </div>
        </div>
      </section>
    )
  }

  <!-- Call for Speakers Section — hidden for now, flip showCallForSpeakers to restore -->
  {
    showCallForSpeakers && (
      <section class="py-10 md:py-14 bg-blue-50 dark:bg-blue-950 dark:text-gray-200">
        <div class="max-w-7xl mx-auto px-4 md:px-6">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
            <div class="mx-auto max-w-xs w-full">
              <img src="/photos/women_panel_ai.png" alt="An illustration for the panel of women" class="rounded-lg" />
            </div>
            <div>
              <h2 class="text-3xl sm:text-4xl font-bold leading-tighter tracking-tighter font-heading text-gray-900 dark:text-slate-100 mb-3">
                Call for Speakers
              </h2>
              <p class="text-lg text-gray-700 dark:text-gray-300 mb-4">
                The HWIB{' '}
                <a href="/about/committees/advocacy" class="text-primary dark:text-blue-400 hover:underline">
                  Advocacy Committee
                </a>{' '}
                is looking for women in the bioinformatics field to join our "Return to Work" panel in October 2026
                (Date TBD).
              </p>
              <p class="text-gray-700 dark:text-gray-300 mb-4">
                We are seeking professionals to share their experiences after having taken a break in their careers,
                whether it was voluntary or involuntary (also includes professionals who have taken maternity leaves).
                We are hoping that your stories will help women going through the same journey right now and give them a
                direction on how they can navigate their careers.
              </p>
              <p class="text-gray-700 dark:text-gray-300 mb-4">
                Application deadline: <strong>August 31, 2026 at 11:59 pm EST</strong>
              </p>
              <a
                href="https://docs.google.com/forms/d/e/1FAIpQLScctmpIoZAUm_UgvOlpy2sovMamLJf7FsFjZxl9fLL9liFkUw/viewform?usp=header"
                target="_blank"
                rel="noopener noreferrer"
                class="btn-primary"
              >
                Apply to Be a Panelist
              </a>
            </div>
          </div>
        </div>
      </section>
    )
  }

  <!-- Community Survey Section -->
  <section class="py-16 md:py-20 bg-white dark:bg-dark">
    <CallToAction
      title="Help Us Close the Gender Gap<br/><span class='text-2xl font-normal'>(and win a $25 gift card!)</span>"
      actions={[
        {
          variant: 'primary',
          target: '_blank',
          text: 'Fill out our survey',
          href: 'https://forms.gle/jhKy3hpYPf1WtC2s8',
          icon: 'tabler:checkbox',
        },
      ]}
    >
      <Fragment slot="subtitle">
        <p class="text-gray-700 dark:text-slate-300 mb-6">
          Our data already shows it: <span class="text-accent dark:text-white font-semibold">21% of men</span> vs. <span
            class="text-accent dark:text-white font-semibold">14% of women</span
          > report holding leadership positions.<br />
          Help us build the evidence base our community deserves.
        </p>
        <ul class="text-gray-700 dark:text-slate-300 text-base space-y-2 mb-2 inline-block text-left">
          <li>✅ Takes ~5 minutes &amp; all responses are anonymous</li>
          <li>🎁 Every 100 new responses, we raffle a $25 gift card</li>
          <li>📢 Share with colleagues, collaborators, and friends</li>
        </ul>
      </Fragment>
    </CallToAction>
    <p class="text-center mt-2">
      <a
        href={`${getPermalink('/resources/survey-results')}?unlocked=true`}
        class="text-sm font-semibold text-primary dark:text-sky-300 hover:underline"
      >
        I've already submitted my data →
      </a>
    </p>
  </section>

  <!-- Let's Connect Section -->
  <section class="py-10 md:py-14 bg-gray-50 dark:bg-slate-800 dark:text-gray-200">
    <Features2
      title="Let's Connect!"
      subtitle="Join our community and stay in touch"
      columns={2}
      classes={{
        headline: {
          title: 'text-3xl font-bold tracking-tight sm:text-4xl mb-4',
          subtitle: 'text-xl dark:text-gray-200',
        },
      }}
      items={[
        // {
        //   title: 'General support',
        //   description: `Chat with us for inquiries related to account management, website navigation, payment issues, accessing purchased templates or general questions about the website's functionality.`,
        // },
        // {
        //   title: 'Contact sales',
        //   description:
        //     'Chat with us for questions about purchases, customization options, licensing for commercial use, inquiries about specific template, etc.',
        // },
        // {
        //   title: 'Technical support',
        //   description:
        //     'Chat with us when facing issues like template installation, problems editing difficulties, compatibility issues with software or download errors, or other technical challenges related to using the templates.',
        // },
        {
          title: 'Discord',
          description: 'Join our Discord community',
          icon: 'tabler:brand-discord',
          callToAction: {
            target: '_blank',
            href: DISCORD_URL,
          },
        },
        {
          title: 'Email',
          description: 'Reach out to our team',
          icon: 'tabler:mail',
          callToAction: {
            target: '_blank',
            href: `mailto:${CONTACT_EMAIL}`,
          },
        },
        {
          title: 'LinkedIn',
          description: 'Connect with us on LinkedIn',
          icon: 'tabler:brand-linkedin',
          callToAction: {
            target: '_blank',
            href: LINKEDIN_URL,
          },
        },
        {
          title: 'Events Calendar',
          description: 'View our upcoming events',
          icon: 'tabler:calendar-event',
          callToAction: {
            target: '_blank',
            href: LUMA_URL,
          },
        },
      ]}
    />
  </section>
  <!-- <Fragment slot="image">
      <YouTube id="gxBkghlglTg" title="Astro just Launched.... Could it be the ultimate web framework?" />
      <style is:inline>
        lite-youtube {
          margin: 0 auto;
          max-width: 100%;
        }
      </style>
    </Fragment> -->
  <!-- </Hero> -->

  <!-- Features2 Widget ************** -->
  <!-- 
  <Features2
    title="About us"
    subtitle="We believe in the magic of turning dreams into stunning realities. Founded by passionate developers with a shared vision, we set out to simplify the website creation process. Our templates bring together the innovation of Astro 4.0 and the versatility of Tailwind CSS, enabling you to express your unique brand identity like never before."
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Features2> -->

  <!-- Stats Widget ****************** -->
  <!-- 
  <Stats
    title="Discover the impressive impact of Astrowind"
    subtitle="The numbers below reflect the trust our users have placed in us and the remarkable outcomes we've helped them achieve."
    stats={[
      { title: 'Downloads', amount: '182K' },
      { title: 'Websites Launched', amount: '87' },
      { title: 'User Ratings', amount: '4.8' },
      { title: 'Satisfied Clients', amount: '116K' },
    ]}
  /> -->

  <!-- Brands Widget ****************** -->

  <!-- <Brands
    title="Partnerships & Collaborations"
    subtitle="At Astrowind, we believe in the power of collaboration to drive innovation and create exceptional experiences."
    icons={[]}
    images={[
      {
        src: 'https://cdn.pixabay.com/photo/2015/05/26/09/37/paypal-784404_1280.png',
        alt: 'Paypal',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2021/12/06/13/48/visa-6850402_1280.png',
        alt: 'Visa',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2013/10/01/10/29/ebay-189064_1280.png',
        alt: 'Ebay',
      },

      {
        src: 'https://cdn.pixabay.com/photo/2015/04/13/17/45/icon-720944_1280.png',
        alt: 'Youtube',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2013/02/12/09/07/microsoft-80658_1280.png',
        alt: 'Microsoft',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2015/04/23/17/41/node-js-736399_1280.png',
        alt: 'Node JS',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2015/10/31/12/54/google-1015751_1280.png',
        alt: 'Google',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2021/12/06/13/45/meta-6850393_1280.png',
        alt: 'Meta',
      },
      {
        src: 'https://cdn.pixabay.com/photo/2013/01/29/22/53/yahoo-76684_1280.png',
        alt: 'Yahoo',
      },
    ]}
  /> -->

  <!-- Features2 Widget ************** -->

  <!-- <Features2
    title="What services do we provide?"
    subtitle="We offer a wide range of website templates that suit various industries and purposes such as business, portfolio, e-commerce, blog, etc."
    items={[
      {
        title: 'Installation Instructions',
        description:
          'Offer clear instructions on how to download the purchased templates and install them on various website platforms or content management systems.',
        icon: 'flat-color-icons:document',
      },
      {
        title: 'Demo and Previews',
        description:
          'Provide interactive demos and previews that allow customers to see how their chosen template will look and function before making a purchase.',
        icon: 'flat-color-icons:template',
      },
      {
        title: 'Technical Support',
        description:
          'Providing customer support for any technical issues related to the templates or their implementation.',
        icon: 'flat-color-icons:voice-presentation',
      },
    ]}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Features2> -->

  <!-- Features Widget *************** -->

  <!-- <Features
    id="features"
    title="Main features of our templates"
    subtitle="Possess several key characteristics to effectively cater to the needs of startups and entrepreneurs."
    columns={3}
    items={[
      {
        title: 'Modern and Professional Design',
        description:
          'Have a contemporary design that reflects current design trends and gives a professional impression.',
        icon: 'tabler:artboard',
      },
      {
        title: 'Responsive and Mobile-Friendly',
        description: 'Adapt seamlessly to different screen sizes and devices to ensure a consistent experience.',
        icon: 'tabler:picture-in-picture',
      },
      {
        title: 'Customizability',
        description:
          'Easily customizable, allowing users to adapt the design, colors, typography, and content to match their brand identity.',
        icon: 'tabler:adjustments-horizontal',
      },
      {
        title: 'Fast Loading Times',
        description: 'Optimized for speed to ensure a smooth user experience and favorable search engine rankings.',
        icon: 'tabler:rocket',
      },
      {
        title: 'Search Engine Optimization (SEO)',
        description:
          'Incorporate SEO best practices in template structure and code to improve visibility in search engine results.',
        icon: 'tabler:arrows-right-left',
      },
      {
        title: 'Compatibility',
        description: 'The templates work seamlessly across various content management systems and website builders.',
        icon: 'tabler:plug-connected',
      },
    ]}
  /> -->

  <!-- FAQs Widget ******************* -->

  <!-- <FAQs
    title="Frequently Asked Questions"
    items={[
      {
        title: 'What are landing page templates?',
        description:
          'Landing page templates are pre-designed web page layouts that are specifically created to serve as a foundation for building effective landing pages. These templates are designed to capture the attention of visitors and guide them towards a specific action or goal, such as signing up for a newsletter, making a purchase, or downloading a resource.',
      },
      {
        title: 'Why should I use a template?',
        description:
          'Some of the advantages are that they provide a ready-to-use structure,  saving you significant time. Are designed with user-friendliness in mind and provide a cost-effective alternative, saving you money while still delivering a quality result.',
      },
      {
        title: 'Can I preview templates before buying?',
        description:
          'Yes, the templates allow you to preview them before making a purchase. There is a "Demo" button associated with each template.',
      },
      {
        title: 'Do I need technical skills to use a template?',
        description:
          'Advanced technical skills are not required to use a template, but having a basic understanding of web navigation and familiarity with using online tools can still be beneficial. If you have more specific customization needs, you might need to consult guides or reach out to customer support for assistance.',
      },
      {
        title: 'Can I use the template on multiple websites?',
        description:
          'No, the template comes with a single-use license, meaning you can use the template on one website or project only. Using the template on additional websites would require purchasing additional licenses.',
      },
      {
        title: 'What if I need help with customization?',
        description:
          "The templates provides a comprehensive step-by-step guide that walk you through the customization process. If you still have doubts, you can reach out to our customer support team. They can answer your questions, provide guidance on customization, and address any issues you're facing.",
      },
    ]}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </FAQs> -->

  <!-- Features3 Widget ************** -->

  <!-- CallToAction Widget *********** 

  <CallToAction
    actions={[
      {
        variant: 'primary',
        target: '_blank',
        text: 'Join us',
        href: 'https://www.meetup.com/houston-women-in-bioinformatics/',
        icon: 'tabler:download',
      },
    ]}
  >
    <Fragment slot="title">Be a part of our vision</Fragment>

    <Fragment slot="subtitle">
      Discover a dynamic work environment, unparalleled growth opportunities, and the chance to make a meaningful
      impact.
    </Fragment>
  </CallToAction>
</Layout>
-->
</Layout>
